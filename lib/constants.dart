import 'package:flutter/material.dart';

const primaryColor = Color(0xFFFFC107);
const secondaryColor = Color(0xFFE2E2E2);
const darkColor = Color(0xFF8E8C8C);
const bodyTextColor = Color(0xFF565A5E);
const bgColor = Color(0xFFE0DBDB);
const cardProjectColor = Color(0xFFDEDBDA);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // we use it on our animation
const maxWidth = 1440.0; // max width of our web
